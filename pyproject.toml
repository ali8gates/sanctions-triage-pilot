[project]
name = "sanctions-triage"
version = "0.1.0"
description = "Illustrative sanctions and PEP alert triage pilot, synthetic data only."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
